---
// src/components/CustomThemeSelect.astro
// Zde můžete naimportovat ikony nebo původní komponentu pro inspiraci
---

<button id="theme-toggle" aria-label="Přepnout režim">
  <span class="light-icon">☀️</span>
  <span class="dark-icon">🌙</span>
</button>

<script is:inline>
  const theme = (() => {
    if (typeof localStorage !== 'undefined' && localStorage.getItem('starlight-theme')) {
      return localStorage.getItem('starlight-theme');
    }
    return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
  })();

  const setEmojiTheme = (t) => {
    document.documentElement.dataset.theme = t;
    localStorage.setItem('starlight-theme', t);
  };

  document.getElementById('theme-toggle').addEventListener('click', () => {
    const currentTheme = document.documentElement.dataset.theme;
    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
    setEmojiTheme(newTheme);
  });
</script>

<style>
  /* Vlastní styly pro váš přepínač */
  #theme-toggle { border: none; background: transparent; cursor: pointer; font-size: 1.5rem; }
  :global([data-theme='light']) .dark-icon { display: none; }
  :global([data-theme='dark']) .light-icon { display: none; }
</style>
